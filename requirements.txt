# Build-time only. Nothing here is shipped to the browser or needed to deploy —
# GitHub Pages serves app/ as static files. See CLAUDE.md.
#
# tools/build_catalog.py uses urllib from the standard library and needs
# nothing here.
#
# tools/slice_streak_icons.py slices the consistency-rank artwork. Build time
# only — the six WebP files it writes are committed, and the browser only ever
# sees those.
Pillow>=10.0

# Phase 8 (optional self-hosted API) will add:
#   Flask>=3.0
#   SQLAlchemy>=2.0
#   alembic>=1.13
