# Build-time only. Nothing here is shipped to the browser or needed to deploy —
# GitHub Pages serves docs/ as static files. See CLAUDE.md.
#
# tools/build_catalog.py uses urllib from the standard library, so this file is
# currently empty of hard requirements. It exists so `pip install -r` is always
# the right first step, and so Phase 8's Flask deps have somewhere to land.
#
# Phase 8 (optional self-hosted API) will add:
#   Flask>=3.0
#   SQLAlchemy>=2.0
#   alembic>=1.13
